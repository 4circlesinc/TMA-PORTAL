package com.tmantoinelaw.portal.web

/** Whether the window is in front: a push that arrives then is the socket's to show (desktop: the panel only when not focused). */
object AppForeground {
    @Volatile var resumed: Boolean = false
}
